"""Enumeration of logical database names used in the project."""

from enum import Enum


class DatabaseEnum(Enum):
    """Unique identifiers for databases managed by the application."""

    market_data_lakehouse_orchestrator = 1
