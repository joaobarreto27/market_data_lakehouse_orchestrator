"""HTTP base endpoint configurations."""

from enum import Enum


class HttpBaseEnum(Enum):
    """Constants for base API endpoints used by the HTTP utilities."""

    api_endpoint = "https://brapi.dev/api/quote"
